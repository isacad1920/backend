// Deprecated legacy ESLint config. Using flat config at `eslint.config.cjs`.
// Retained temporarily so editors that still look for .eslintrc.* don't error.
// Safe to delete once all tooling respects the flat config.
module.exports = {};
